<%= render "layouts/message" %>

<div class="panel panel-default">
  <div class="panel-body">
  	<h2>Show Contact Field</h2>
    <p>
		  <strong>Name:</strong>
		  <%= @contact_type.name %>
		</p>

		<br>
		<%= link_to 'Edit', edit_contact_type_path(@contact_type), :class => "btn btn-primary btn-block" %>
		<%= link_to 'Back', contact_types_path, :class => "btn btn-default btn-block" %>
  </div>
</div>
